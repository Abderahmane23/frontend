1️⃣ Responsive Design Fundamentals (Core Rules)
✅ Design for width, not inches

Physical screen size varies, but usable width (dp/pt) is what matters.

Recommended width buckets:

Compact: ≤360dp (older/smaller phones)

Standard: 360–411dp (majority of devices)

Large: ≥412dp (6.6″–6.8″ phones)

➡️ Layouts should reflow, not resize.

2️⃣ Use Flexible Layouts (Mandatory)
✔ Auto-adjustable stacks & grids

Prefer vertical stacks that expand naturally

Switch to 2-column grids only when width allows

Let content wrap instead of truncating aggressively

Examples

Lists → single column (default)

Cards → 2 columns only on wider screens

Forms → stacked fields (never side-by-side on phones)

✔ Orientation-aware layouts

Portrait: vertical flow

Landscape: wider margins, optional two-pane views

Never lock orientation unless required (e.g., camera apps)

3️⃣ Avoid Fixed Pixel Values (Critical)
❌ Don’t use:

Fixed px widths/heights

Absolute positioning for content

✅ Use instead:

Relative units

Android: dp, sp, weight

iOS: Auto Layout constraints, Spacer(), GeometryReader

Percent-based spacing

Flex ratios for width distribution

➡️ This ensures consistency across 6.1″–6.8″ without manual tuning.

4️⃣ Extend Layouts Responsibly (Safe Areas)
📌 Edge & thumb-zone rules

Keep primary actions ≥16–24dp away from screen edges

Avoid placing critical buttons in:

Bottom corners

Near gesture areas (home/back swipes)

📌 Safe area handling

Respect notches, punch holes, rounded corners

Use system-provided safe area APIs

Bottom navigation bars must stay fully reachable by thumb

➡️ Usability > screen occupation

5️⃣ Visual Simplicity = Performance
❌ Avoid over-complex visuals

Heavy shadows

Multiple layered transparencies

Blur effects on scrolling content

Large animated gradients

✅ Prefer:

Flat or subtle elevation

1 shadow layer max (or none on low-end devices)

Solid backgrounds

Simple transitions (fade, slide)

➡️ Especially important for Tecno / Itel / Infinix hardware.

6️⃣ Caching Strategy (Essential)
✔ What to cache

Images (thumbnails & avatars)

API responses

Static configuration data

Fonts & icons

✔ Best practices

Memory cache + disk cache

Cache by screen size & resolution

Use cache-first strategy, then update in background

✔ Benefits

Faster load times

Lower data usage

Better experience on unstable networks

7️⃣ Progressive Loading (Must-Have)
✔ Load content in stages

Skeleton UI / placeholders

Text first

Images after

Heavy content last (charts, videos)

✔ Techniques

Lazy load images below the fold

Low-resolution image first → upgrade when loaded

Paginate long lists (never load everything at once)

➡️ Users should see something within the first second.

8️⃣ Performance-First UI Decisions

For 6.1″–6.8″ mid-range devices:

Limit animation duration (<300ms)

Avoid continuous animations

Reduce redraws & layout passes

Pre-measure dynamic layouts where possible

✅ Final Checklist (Use This Before Release)

✔ Layout adapts from 360dp → 430dp+
✔ No fixed pixel dimensions
✔ Safe areas respected (notches, gestures)
✔ Primary actions reachable by thumb
✔ Visuals remain simple and flat
✔ Caching enabled for images & data
✔ Progressive loading implemented
✔ Smooth on low-end devices